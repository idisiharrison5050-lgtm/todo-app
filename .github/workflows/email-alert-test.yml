name: Email Alert Test

on:
  workflow_dispatch:

jobs:
  failure-test:
    name: Intentional failure for email testing
    runs-on: ubuntu-latest
    steps:
      - name: Trigger a controlled failure
        run: |
          echo "This workflow intentionally fails to verify GitHub Actions failure notifications."
          exit 1
